import React from 'react'
import { StyleSheet, Text, View } from 'react-native'


export default function UnitsSlider(item: any) {
    return (
        <View>
            <Text>{item}</Text>
        </View>
    )
}

const styles = StyleSheet.create({})
