import { StyleSheet, Text, View } from 'react-native'

import React from 'react'

export default function TextInput(item: any) {
    return (
        <View>
            <Text>{item}</Text>
        </View>
    )
}

const styles = StyleSheet.create({})
