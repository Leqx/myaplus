import { StyleSheet, Text, View } from 'react-native'

import React from 'react'

export default function AppSheet(item: any) {
    return (
        <View>
            <Text>{item}</Text>
        </View>
    )
}

const styles = StyleSheet.create({})
