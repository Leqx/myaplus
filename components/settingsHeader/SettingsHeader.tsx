import { StyleSheet, Text, View } from 'react-native'

import React from 'react'

export default function SettingsHeader(item: any) {
    return (
        <View>
            <Text>{item}</Text>
        </View>
    )
}

const styles = StyleSheet.create({})
